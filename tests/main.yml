---
- hosts: localhost
  become: yes
  roles:
    - role: isset.package
      package_name: htop

  tasks:
    - command: dpkg -l htop
      register: dpkg

    - assert:
        that:
          - '"no packages found matching" not in dpkg.stdout'
          - '"ii" in dpkg.stdout'